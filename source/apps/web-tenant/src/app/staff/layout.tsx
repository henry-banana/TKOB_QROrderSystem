'use client';

/**
 * Staff Layout Wrapper
 * StaffPage is a full-screen component that manages its own layout
 */
export default function StaffLayoutWrapper({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
